interface:
  display_name: "Codex MCP Guard"
  short_description: "Read-only MCP candidate audit; no cleanup"
  default_prompt: "Use $codex-mcp-guard:codex-mcp-guard to audit candidate MCP helpers without asserting ownership."
